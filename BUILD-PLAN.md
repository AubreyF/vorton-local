# Overnight build and continuity

## Owner decisions, September 13, 2026

Vorton Local supersedes the larger cloud platform as the immediate delivery path. Vorton Cloud remains available with its unfinished work preserved. The new GitHub repository is private. The target is a similar MacBook Pro, at `~/dev/vorton`, containing AubOS and FreedOS. Literal `/dev` is not a target. HTTPS through Tailscale is already solved and must not be changed.

One owner. No Supabase, hosted identity, cloud infrastructure, or new API subscriptions. Use the destination's authenticated local Codex installation. Preserve a hard 50 percent weekly allowance reserve. No source-side AubOS changes. Prepare a single copyable directory and one destination continuity prompt.

## Scope correction, September 14

The owner has an active destination task adding resource consumption control to Paseo's scheduler. **Do no FreedOS factory-specific work in this build.** Do not build a factory runner, issue admission, factory scheduling, or a competing budget scheduler. Earlier factory deliverables in conversation and goal metadata are superseded. FreedOS receives the shared Goals, Tasks, and recommendation interface only. Future execution integrates with the finalized Paseo contract, not an invented adapter.

## Delivery order

1. Separate profile state, durable atomic writes, optimistic concurrency, activity history, and hostile scope tests.
2. Goals, milestones, linked tasks, recommendation review and accept/edit/defer/reject.
3. Authentic AubOS theme registry, typography, controls, one header, avatar menu, and workspace dropdown without decorative double borders.
4. Executive evidence packets, explicit role review, durable recommendation status, and no automatic acceptance. No scheduler.
5. Preserve a clean integration boundary for the destination Paseo scheduler. Do not implement the factory.
6. Startup, health, backup, isolated restore, destination attachment, and a complete continuity prompt.
7. Privacy scan, tests, browser proof, private commit/push, and exact handoff record.

## Important implementation distinction

The new local Goals store is not a silent replacement for AubOS strategic registers. Existing AubOS pages and source data remain available. Any import must be explicit, preserve source IDs, and avoid creating two authoritative writers for the same record.

The current authoritative AubOS runtime is preserved on the destination and attached as an internal installation. This package must not ship stale personal data. Any integration requiring its actual destination paths is verified by the destination agent.

## Deferred

Advanced memory, cloud tenancy, plugin distribution, remote worker fleets, and general installation migration are not prerequisites for this pilot.

## Required follow-up after delivery

After the package and continuity prompt are complete, create a new user-visible task to inventory all paid cloud services and servers established for the original Vorton Cloud. Include billing evidence where accessible, dependency checks, owner/account identity without secrets, and a proposed shutdown order. This is read-only discovery, not authorization to cancel subscriptions or delete resources. Future server work should favor the owner's specialized Paseo host rather than reviving the old cloud stack. Do not launch this task before the requested local delivery is finished.

## Current state

Path correction: use `~/dev/vorton` on both Macs, without a local suffix. The development cloud checkout was moved to `~/dev/vortoncloud` with its uncommitted changes preserved and linked worktrees repaired. Package source and personal application attachment remain separate.

The local pilot is implemented. GitHub `AubreyF/vorton-local` remains private. Existing GitHub repository was renamed to `AubreyF/vorton-cloud`; its dirty worktree is preserved. `START-HERE.md` is the destination continuity prompt. `docs/VALIDATION.md` records development evidence and the checks that remain on the authoritative destination. No factory or scheduler was implemented.
