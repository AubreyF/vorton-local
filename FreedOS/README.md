# FreedOS installation

Local core state is stored in `state/`. Factory worktrees, repository configuration, run output, and credentials are not committed. The factory starts paused. Coding actions require explicit issue admission and the existing Freed task-claim contract.
