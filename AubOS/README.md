# AubOS installation

Local core state is stored in `state/`. It starts empty and is ignored by Git.

On the destination, `authoritative/` holds the existing working AubOS application and its authoritative files. It is never populated from stale development-machine personal data. The destination handoff will verify preservation before attachment. This directory is not a request to migrate strategic records into the new Goals store.
